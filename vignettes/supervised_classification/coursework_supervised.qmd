---
title: "Building a classifier to predicting house age"
author: "Anonymous"
format: 
  html:
    embed-resources: true

---
## The Task
The goal of this project is to look at the environmental impact because homes built before and after 1930. This report will also build a model to be able to accurately predict when a home was built based off of the environmental impact data. This will be beneficial because it will allow officials to approach these homes with a different strategy that is needed for reducing the environmental impact of UK housing. 

## The Problem
Climate change is an every increasing issue in the world and it is important to reduce the amount of carbon emissions in all aspects of life, including housing. Since housing has a multitude of variables that impacts it's energy efficiency and carbon footprint, it makes sense that a different approach to reduce the environmental impact will be different for different types of homes. Homes can be separated out into two groups, homes built before 1930 and homes built after. This makes sense because older homes where built differently and are more settled so drafts are more of an issue. 

The data used in this report is a non-random sample of UK housing from [Energy Certificates](https://epc.opendatacommunities.org/) and was downloaded December of 2025. The full dataset is only available on server '10.121.4.250' with the file 'energy.energy_certificates'. There are 93 total variables which include information on: 

- Property energy efficiency -- The data provides a ranking from A (most efficient)  to G (least efficient)
- CO~2~ Emissions
- Electricity costs
 
## The Goal
The goal of this report is to determine if it is possible to categorize how old a home is (pre and post 1930) based off of energy efficiency data and the metrics: `current_energy_efficiency`, `environment_impact_current`, `energy_consumption_current`, `co2_emissions_current`, `lighting_cost_current`, `heating_cost_current`, `hot_water_cost_current`. If able to predict the age of the home based off of the data will save a lot of resources because having a home visit to gather that information would not be needed.


## The Data
### Data Preview
First, a preview of the data is great because it provides and basic overview of that type of variables and see the size of the data. Below is a table detailing the amount of data points for each group, houses built before and after 1930.

```{python}
#| echo: false
import pandas as pd
```



```{python}
#| echo: false
#| results: 'asis'
df = pd.read_csv('../../data_cache/vignettes/supervised_classification/frequencies.csv')
df.round(3)
```


Below are more summary statistics. 

```{python}
#| echo: false
#| results: 'asis'
df = pd.read_csv('../../data_cache/vignettes/supervised_classification/grouped_stats.csv')
df.round(3)
```


### Scatter plots
It is always useful to view a scatter plot of the data, marking the two known groups where the points in red are homes built after the 1930 and the blue points are homes built before then. 


![](../../data_cache/vignettes/supervised_classification/scatterplot.html){width="100%" height="600px"}

The scatter plot should a linear trend where the more energy efficient the home is, the more of an environmental impact there will be. This seems counter intuitive where one would think that efficiency would mean that there is less of an environmental impact. It is also the more modern homes that have this result.  

![](../../data_cache/vignettes/supervised_classification/boxplot_matrix.html){width="100%" height="600px"}

The three boxplots above show the current energy efficiency, current environmental impact and the current amount of CO~2~ emissions of homes built before and after 1930; Where even though on average the modern homes have more energy efficiency they have a higher environmental impact even though the average amount of carbon emissions coming from the homes are relatively similar. It is also interesting that the older homes have a large variance of carbon emissions. 

## Fitting LDA and QDA classifiers

The simplest classifiers we could apply are Linear and Quadratic Discriminant analysis (LDA and QDA respectively). LDA uses Bayes# theorem to calculate the probability if an input will belong to a specific output grouping. QDA is a more complex method that is similar to LDA but additionally uses its own covariance matrix. Both methods are extremely useful; however, sometimes over complicating the model can do more harm than good. Therefore, it is best to compare the two models to determine which one is the better fit for predicting the age of a home. 

### Results from fitting a linear discriminant analysis

```{python}
#| echo: false
#| results: 'asis'
df = pd.read_csv('../../data_cache/vignettes/supervised_classification/lda.csv')
df.rename(columns={"Unnamed: 0": "Category"}, inplace=True)
print(df.round(3).to_markdown(index=False))
```

### Results from fitting a quadratic discriminant analysis

```{python}
#| echo: false
#| results: 'asis'
df = pd.read_csv('../../data_cache/vignettes/supervised_classification/qda.csv')
df.rename(columns={"Unnamed: 0": "Category"}, inplace=True)
print(df.round(3).to_markdown(index=False))
```

You should comment on these results. You may wish to add an interpretation of the coefficients of these classifiers.  Or there may be other metrics (confusion matrix) that you think should be presented.

A confusion matrix for LDA and QDA is a great why to compare the two models on their accuracy, precision, and recall. 

```{python}
#| echo: false
#| results: 'asis'
df_lda = pd.read_csv('../../data_cache/vignettes/supervised_classification/lda.csv')
df_qda = pd.read_csv('../../data_cache/vignettes/supervised_classification/qda.csv')
df_lda.rename(columns={"Unnamed: 0": "Category"}, inplace=True)
df_qda.rename(columns={"Unnamed: 0": "Category"}, inplace=True)
# Merge predictions by Category
df = df_lda.merge(df_qda, on="Category", suffixes=("_lda", "_qda"))

print(df.head())
```


## ROC curve (and AUC) for two simple classifiers

The Receiver Operating Characteristic or ROC curve is a model that determines how reliable the model is at differentiating between two classifiers. True Positive Rate (TPR) is the proportion of how many times the model is correct in identifying a positive case as such. Conversely, False Positive Rate (FPR) is the proportion of cases that are negative but where identified as positive. 

The Area Under the Curve (ACU) is an index that determines how well the model is. The scale goes from 0-1 where 1 is perfect and 0.5 is random and anything less than 0.5 is worse than if the model was truly random.

![](../../data_cache/vignettes/supervised_classification/roc.html){width="100%" height="600px"}

Here it can be seen that the performance of the LDA classifier has an AUC of 0.81 and the QDA classifier as a AUC of 0.79. Both of these models are fairly good, however the LDA is slightly better. This 



##  Additional credit

For additional credit you should consider extended the pipeline and trying out other classifiers.


## Record of AI use for MTHM503 supervised coursework

Instructions: You can use this document to record when, how and why you used GenAI to complete your assessment. It will help you create a record of AI use to submit alongside your references for AI-integrated and AI-assisted assignments. It may also be useful to help you discuss your AI use if you are required to do so in an academic conduct meeting.

| **Date**       | **AI tool used** | **Purpose**                                | **Prompt**                                 | **Hyperlink to output (where possible)** | **Section of work used for** |
|----------------|------------------|--------------------------------------------|--------------------------------------------|------------------------------------------|-------------------------------|
| E.g. 15/07/2025| MS Copilot       | To help create an organised essay structure| ‘Generate ideas for structuring an essay on...’ | [insert link]                          | introduction                  |
|                |                  |                                            |                                            |                                          |                               |
|                |                  |                                            |                                            |                                          |                               |
|                |                  |                                            |                                            |                                          |                               |
|                |                  |                                            |                                            |                                          |                               |
|                |                  |                                            |                                            |                                          |                               |
|                |                  |                                            |                                            |                                          |                               |
|                |                  |                                            |                                            |                                          |                               |
|                |                  |                                            |                                            |                                          |                               |

